name: hello-world
on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: echo-hello-world
        run: echo "Hello World!"
